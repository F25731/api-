services:
  yunyi-parser:
    build: .
    container_name: yunyi-parser
    restart: unless-stopped
    ports:
      - "8787:8765"
    environment:
      - NODE_ENV=production
      - PORT=8765
      - ADMIN_PASSWORD=Fyb2530+
    volumes:
      - yunyi-parser-data:/app/data

volumes:
  yunyi-parser-data:
